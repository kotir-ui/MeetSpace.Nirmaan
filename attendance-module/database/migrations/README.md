# Attendance migrations

Keep Attendance-only schema changes in this directory. Apply them in filename order after the baseline `database/schema.sql`.

The starter module has no incremental migration yet; `schema.sql` is the current baseline. Do not place portal migrations here.
