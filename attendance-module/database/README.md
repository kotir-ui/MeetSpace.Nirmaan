# Database

The module targets PostgreSQL. Run `schema.sql` once, then `seed.sql` for sample employees. The `migrations/` directory is reserved for incremental changes; keep each migration numbered and reversible where practical.
